import {Ghost} from "./Ghost";
import {EMF5, Fingerprints, GhostOrb} from "../evidence";

export default {
    name: 'Obake',
    evidence: [
        EMF5,
        GhostOrb,
        Fingerprints
    ]
} as Ghost;